/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

// next.config.js
module.exports = {
    images: {
      domains: ['www.bedigitalbiz.co.th'],
    },
  };
  

  // next.config.js

module.exports = {
  images: {
    domains: ['flowbite.s3.amazonaws.com', 'www.bedigitalbiz.co.th'], // เพิ่มโดเมนที่คุณต้องการอนุญาต
  },
};
